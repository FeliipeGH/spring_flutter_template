import 'package:flutter/material.dart';

class NotAuthPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("No autorizado"),
      ),
    );
  }
}
