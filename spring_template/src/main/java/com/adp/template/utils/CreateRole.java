package com.adp.template.utils;
/*
import com.adp.template.security.enums.RoleEnum;
import com.adp.template.security.model.AppRole;
import com.adp.template.security.service.AppRoleService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

@Component
public class CreateRole implements CommandLineRunner {

    @Autowired
    private AppRoleService appRoleService;

    @Override
    public void run(String... args) throws Exception {
        AppRole appRoleAdmin=new AppRole(RoleEnum.ROLE_ADMIN);
        AppRole appRoleUser=new AppRole(RoleEnum.ROLE_USER);
        appRoleService.save(appRoleAdmin);
        appRoleService.save(appRoleUser);
    }
}
*/
public class CreateRole{
}